# Agent Rules — convergio-mcp

Tool-agnostic. Applies to Claude Code, Cursor, Copilot, Windsurf, any AI tool.

## Build

```bash
cargo fmt --all -- --check
RUSTFLAGS="-Dwarnings" cargo clippy --workspace --all-targets --locked
cargo test --workspace --locked
```

Zero tolerance: 0 warnings, 0 skipped tests, 0 TODO without issue link.

## Structure

```
crates/convergio-mcp/
├── src/            — source code
│   └── lib.rs      — public API, module declarations, unit tests
├── tests/          — integration + adversarial tests
docs/adr/           — architecture decision records
```

## Rules

- English only (code + docs)
- Max 300 lines per file
- Conventional commits: `feat:`, `fix:`, `docs:`, `chore:`, `refactor:`
- Every extension owns its DB tables via `migrations()`
- Breaking changes = minor version bump (pre-1.0)
- PR requires 5 sections: Problem, Why, What changed, Validation, Impact
- SDK types come from convergio-sdk — never duplicate, always import

## Test requirements

| Type | Location | When required |
|------|----------|---------------|
| Unit | `src/*.rs` `#[cfg(test)]` | Always |
| Integration | `tests/integration_*.rs` | Always |
| Adversarial | `tests/adversarial_*.rs` | If crypto/auth/network |
| Responsible AI | `tests/responsible_ai_*.rs` | If LLM/agent |
| Accessibility | `tests/accessibility_*.rs` | If UI |

Coverage minimum: 70% (SDK: 80%). Enforced by CI.

Test rules:
- Never hardcode counts (`>=` not `==`)
- Never hardcode versions (use `env!("CARGO_PKG_VERSION")`)
- Never skip tests without issue link

## Comments in code

Only 2 types allowed:
- `//!` module doc — 1-2 lines: what it does, who uses it
- `// WHY:` — only when choice is non-obvious from code

Forbidden: `/// Returns the name`, `// increment counter`, `/// # Examples`.
Agent reads code for "what", tests for "how to use it".

## Documentation

- README.md: badge, description, usage, API summary, manifesto
- CHANGELOG.md: auto-generated by release-please
- docs/adr/: one ADR per non-obvious decision
- Doc comments on public types only when behavior is non-obvious

## Model routing

| Role | Model |
|------|-------|
| Architecture, security, planning, review | Opus |
| Mechanical execution (CRUD, tests, file ops) | Sonnet |
| Classification, routing | Haiku |

## Do NOT

- Ship without unit + integration tests
- Merge PRs without user approval
- Modify SDK types here (changes go in SDK repo)
- Add deps without checking SDK version alignment
- Bypass hooks, tests, or CI gates
- Leave warnings, TODO without issue, or dead code

## Engineering fundamentals (from Microsoft ISE playbook)

- Source control: conventional commits, branch protection, no force push
- Testing: unit + integration + adversarial, coverage gate
- CI/CD: fmt + clippy + test + audit + deny + coverage
- Security: cargo-audit, cargo-deny, secret scanning, pentest on release
- Code review: structured PR, review before merge
- Observability: SDK telemetry, health check, structured logging
